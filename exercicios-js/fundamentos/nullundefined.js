/* diferença entre passagemd e parametros por valor ou por referencia
    valor para elementos básicos do js, como números
        uma mudança no valor da 2a variável não altera o valor da 1a
    referencia para coisas como objetos, funções etc
        uma mudança na região apontada pela 2a variável interfere na 1a variável também
*/

let valor // sem atribuição
console.log(valor); // valor undefined por padrão
// console.log(valor2); // is not defined, gera erro

